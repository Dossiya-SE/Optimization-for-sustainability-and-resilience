# Pending One-Time Setup

The repository is operational without external secrets.

One optional capability remains pending: AI-assisted mathematical audits through GitHub Actions.

To enable it, create an OpenAI Platform project API key and store it in GitHub Actions as the repository secret `OPENAI_API_KEY`. Never commit the key to source control.

Until that secret is configured, deterministic repository checks, Julia verification, LaTeX/TikZ compilation, and the Xmind knowledge architecture remain available.
