# Power-Water-Transportation Case Study

This directory will host the coupled infrastructure case study after single-sector models are validated.

The intended modeling architecture is

```math
\text{sector states}\rightarrow\text{cross-sector interfaces}\rightarrow\text{service constraints}\rightarrow\text{optimization decisions}.
```

The first implementation should identify coupling variables and physical units before adding resilience objectives.
